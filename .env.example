# GEMINI_API_KEY: Required for Gemini AI API calls.
# AI Studio automatically injects this at runtime from user secrets.
# Users configure this via the Secrets panel in the AI Studio UI.
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# DATABASE_URL: Postgres connection string used by Vercel serverless API routes.
# On Vercel, you can set DATABASE_URL directly or use a Vercel Postgres integration
# (this app also accepts POSTGRES_URL / POSTGRES_PRISMA_URL / POSTGRES_URL_NON_POOLING).
DATABASE_URL="postgres://USER:PASSWORD@HOST:5432/DBNAME?sslmode=require"

# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"

# Optional: Override the admin password used for the hidden roster page.
# If omitted, the app falls back to the built-in default.
ADMIN_ACCESS_CODE="Umphress1997!"
